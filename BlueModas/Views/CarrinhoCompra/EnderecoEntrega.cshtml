@using BlueModas.Libraries.Pagination
@using BlueModas.Models
@model EnderecoEntrega
@{ ViewData["Title"] = "EnderecoEntrega";

decimal? total = 0;

foreach (var produto in ViewBag.Produtos)
{
    total += produto.Valor * produto.QuantidadeCarrinhoProduto;
} }

<form asp-action="cadastroenderecoentrega" asp-controller="carrinhocompra" method="post">

    <div class="container">
        <br />
        <br />
        <h2 class="title-doc">Identificação do Cliente</h2>

        <div class="row">
            <aside class="col-sm-6">
                <a class="btn btn-outline-primary" asp-action="Index">Voltar</a>
                <br />
                <div id="code_register_1">
                    <div class="card">
                        <article class="card-body">
                            <div class="form-group">
                                <label for="Nome">Nome</label>
                                <input class="form-control" asp-for="Nome" type="text">
                            </div>
                            <div class="form-group">
                                <label for="Nome">Email</label>
                                <input class="form-control" asp-for="Email" type="text">
                            </div>
                            <div class="form-group">
                                <label for="Nome">Telefone</label>
                                <input class="form-control" asp-for="Telefone" type="text">
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn-primary btn-block">Continuar</button>
                            </div>
                        </article>
                    </div>
                </div>
            </aside>

        </div>
    </div>
</form>>
